@model List<IdentityStructureModel.IdentityModels.AppRole>
@{
	ViewData["Title"] = "Index";
}


<h3>Roller</h3>
<hr />

<table class="table table table-bordered table-striped">
	<tr>
		<th>Id</th>
		<th>Rol ismi</th>

	</tr>

	@foreach (var item in Model)
	{
		<tr>

			<td>@item.Id</td>
			<td>@item.Name</td>

		</tr>

	}
</table>

