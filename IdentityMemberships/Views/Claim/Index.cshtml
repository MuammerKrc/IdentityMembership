@model List<System.Security.Claims.Claim>

@{
	ViewData["Title"] = "Index";
}





<h3>Users</h3>
<hr />
<div class="row">
	<a class="btn btn-primary col-3 mx-3" asp-action="AnkaraPolicyAuthorize">AnkaraCityPolicy</a>
	<a class="btn btn-primary col-3 " asp-action="ExpiredDatePolicyAuthorize">ExchangePolicy</a>
</div>

<table class="table table-bordered table-striped">

	<tr>
		<td>Dağıtıcı</td>
		<td>Türü</td>
		<td>Değeri</td>
	</tr>
	@if (Model.Count() == 0)
	{
		<tr>
			<td colspan="3">Hiç bir üye yoktur.</td>
		</tr>
	}
	else
	{
		@foreach (var item in Model)
		{
			<tr>
				<td>@item.Issuer</td>
				<td>@item.Type</td>
				<td>@item.Value</td>
			</tr>
		}
	}
</table>
